<form method="post" action="{{ path('types_of_wto_delete', {'id': types_of_wto.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_of_wto.id) }}">
    <button class="btn btn-primary">Удалить</button>
</form>
