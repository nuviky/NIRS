<form method="post" action="{{ path('types_of_w_t_o_delete', {'id': types_of_w_t_o.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_of_w_t_o.id) }}">
    <button class="btn">Delete</button>
</form>
