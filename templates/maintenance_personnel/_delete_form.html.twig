<form method="post" action="{{ path('maintenance_personnel_delete', {'id': maintenance_Personnel.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance_Personnel.id) }}">
    <button class="btn btn-primary">Удалить</button>
</form>
