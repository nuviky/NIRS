{% extends 'base.html.twig' %}

{% block title %}WorkersSkillLevel{% endblock %}

{% block body %}
    <h1>WorkersSkillLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workers_skill_level.id }}</td>
            </tr>
            <tr>
                <th>WorkerSkillLevel</th>
                <td>{{ workers_skill_level.workerSkillLevel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('workers_skill_level_index') }}">back to list</a>

    <a href="{{ path('workers_skill_level_edit', {'id': workers_skill_level.id}) }}">edit</a>

    {{ include('workers_skill_level/_delete_form.html.twig') }}
{% endblock %}
