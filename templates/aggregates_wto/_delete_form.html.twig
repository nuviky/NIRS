<form method="post" action="{{ path('aggregates_wto_delete', {'id': aggregates_wto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aggregates_wto.id) }}">
    <button class="btn">Delete</button>
</form>
