<form method="post" action="{{ path('aggregates_wto_delete', {'id': aggregates_wto.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aggregates_wto.id) }}">
    <button class="btn btn-primary">Удалить аггрегат ВТО</button>
</form>
