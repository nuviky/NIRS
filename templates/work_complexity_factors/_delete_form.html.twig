<form method="post" action="{{ path('work_complexity_factors_delete', {'id': work_complexity_factor.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить этот элемент?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ work_complexity_factor.id) }}">
    <button class="btn btn-primary">Удалить</button>
</form>
